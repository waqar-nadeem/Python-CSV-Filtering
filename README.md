# Python-CSV-Filtering
Python utility to filter CSV files based on user-defined conditions. Supports multiple column filters with operators like ==, !=, &lt;, >, &lt;=, >=. Reads input CSVs, applies conditions, and outputs a filtered CSV. Uses Python’s csv module, dictionary handling, and list comprehension for efficient data processing.
